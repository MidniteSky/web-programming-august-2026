/*
    Member Manager v5 - members.js  (Day 5 capstone - THE LIVE BUILD)
    =================================================================
    This file is empty on purpose. Today we write it together, live,
    step by step - and by the end of it the Member Manager is a complete
    CRUD app with a professional Bootstrap UI.

    Everything AROUND this file is already finished: members.html has the
    Bootstrap table, the Add-member modal, the Delete-confirm modal and the
    toast, all sitting there waiting for the JavaScript that switches them on.
    Open the page now and look: it's handsome, and it does nothing. That's
    today's job.

    The build runs in six steps:

      Step 1  READ    - fetch the members (GET) and render the table
      Step 2  SEARCH  - the live search box filters what we fetched
      Step 3  CREATE  - the Add-member modal saves a new member (POST)
      Step 4  TOAST   - a "saved!" pop-up, in one small helper
      Step 5  DELETE  - a Delete button per row, confirmed in a modal
      Step 6  UPDATE  - Edit / Save / Cancel, in place in the row (PUT)

    Fallen behind, or something's broken and the class has moved on?
    No drama: the checkpoints/ folder has the finished code for every step.
    Copy the ENTIRE contents of the latest checkpoint over this file and
    you're caught up. (Each checkpoint contains all the steps before it.)

    >>> START THE SERVER FIRST. In a terminal in this folder (the one with
    >>> db.json), run EXACTLY:
    >>>
    >>>     npx json-server@0.17.4 --watch db.json --port 3000
    >>>
    >>> Leave it running. Stop it with Ctrl+C. Reset the data any time by
    >>> stopping the server, copying db.backup.json over db.json, and
    >>> restarting.
*/
